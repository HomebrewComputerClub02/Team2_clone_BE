server:
  port: 8080

spring:
  application:
    name: demo
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: jdbc:mysql://localhost:3306/homebrewtify?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true&rewriteBatchedStatements=true
    username: homebrew
    password: homebrew
    driver-class: com.mysql.cj.jdbc.Driver
#  datasource:
  #    url: jdbc:h2:tcp://localhost/~/test;MODE=MySQL
#    driver-class-name: org.h2.Driver
#    username: sa
#  datasource:
#    url: jdbc:mysql://localhost:3306/homebrewtify?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true&rewriteBatchedStatements=true
#    username: root
#    password: shine2358!
#    driver-class: com.mysql.cj.jdbc.Driver
  batch:
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: update # update / create : 애플리케이션을 시작할 때 데이터베이스를 초기화하고 다시 테이블 생성
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: 10000
        order_inserts: true
        order_updates: true
        format_sql: true # 실행되는 query 보여줌.
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  profiles:
    include:
      - google
jwt:
  header: Authorization
  secret: c2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQtc2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQK
  token-validity-in-seconds: 600 #for test, 15second
